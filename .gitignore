# ── Runtime data (Docker volume mounts) ─────────────────
/src/
/dist/
/data/
apps/api/data/

# ── Kubernetes secrets (never commit real values) ─────────
k8s/secrets.yml

# ── Dependencies ──────────────────────────────────────────
node_modules/

# ── IDE / OS ──────────────────────────────────────────────
.DS_Store
.idea/
.vscode/
*.swp

# ── Secrets ───────────────────────────────────────────────
.env
publish.config.json
photos.json

# ── SQLite ────────────────────────────────────────────────
*.db
*.db-shm
*.db-wal

# ── Claude Code ───────────────────────────────────────────
.claude/
