# ── Build output ─────────────────────────────────────────
dist/

# ── Dependencies ──────────────────────────────────────────
node_modules/

# ── Generated example gallery (recreated by npm run setup:example) ─
src/example-gallery/

# ── User galleries (real photos — do not commit) ──────────
src/*/photos/

# ── IDE / OS ──────────────────────────────────────────────
.DS_Store
.idea/
.vscode/
*.swp

# Publish config (contains server credentials)
publish.config.json
